[package]
name = "minimal_test"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1.44", features = ["rt", "macros"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
tokio-test = "0.4"